import mongoose from "mongoose";

const videoSchema = new mongoose.Schema({
    title:{type:String, required:true, trim: true, ma},
    description:,
    createdAt:,
    hashtags:[],
    meta:{
        views:,
        rating:
    }
});
